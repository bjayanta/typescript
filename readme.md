# Editor Setup & Compilation

Compile typescript file:

> tsc script.ts

Create tsconfig file:

> tsc --init

change tsconfig file:

```json
"rootDir": "./src",
"outDir": "./output",
```

Compile:

> tsc

or,

> tsc -w

Thanks.
